define([
	"jquery",
	"use!backbone",
	"use!underscore",
	"use!ui",
	"use!qtip"
		],
function($, Backbone, _, ui, qtip){

	var TooltipMenuView = Backbone.View.extend({
	});

	return TooltipMenuView;
});
		

